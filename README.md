'input'
 known miRNA-drug interaction;
 known drug-gene interaction;
 drug ID, name, smiles ;
 miRNA ID, name, sequences ;
 Gene Symbol.

'data_preprocess.py' contains the preprocess of data;
'train.py' contains training and testing code;
'main.py'runs DeepMDA;


Here we provide a example:

1. Download and upzip our data and code files
2. Run "data_preprocess.py" to obtain miRNA and drug similairty graph 
3. Run "main.py"
